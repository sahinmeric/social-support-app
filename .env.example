# Backend API Configuration
# Base URL for backend API (defaults to /api for same-origin requests)
# For development with separate backend: http://localhost:3000/api
# For production: /api (same origin)
VITE_API_BASE_URL=/api

# Use mock AI responses for development (no backend required)
# Set to "true" for testing without backend
# Set to "false" to use real backend API
VITE_USE_MOCK_AI=true
